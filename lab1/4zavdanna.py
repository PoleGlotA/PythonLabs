# Запит користувача для введення числа
number = int(input("Введіть число: "))
# Перевірка, чи є число парним чи непарним
if number % 2 == 0:#якщо остача від ділення ні 2 є 0, то число парне
    print(f"{number} є парним числом.")
else:
    print(f"{number} є непарним числом.")